import { StepPage } from "../StepPage";

export default function LldPage() {
  return (
    <StepPage stepId="05-lld">
      <p className="text-xs text-slate-200">
        Break the high-level design into concrete modules, APIs, and data
        structures. Focus on the pieces needed to eventually build the AI
        Resume Builder, but don&apos;t implement them yet.
      </p>
    </StepPage>
  );
}

