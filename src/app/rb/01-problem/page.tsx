import { StepPage } from "../StepPage";

export default function ProblemPage() {
  return (
    <StepPage stepId="01-problem">
      <p className="text-xs text-slate-200">
        Capture the core problem this AI Resume Builder project is solving.
        Describe the user, their pain, and how success will be measured. Keep it
        concise so it fits well into the build track flow.
      </p>
    </StepPage>
  );
}

